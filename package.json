{
  "name": "sf6-replay-bridge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check api/state.js && node --check api/upload.js && node --check api/matches.js",
    "test": "npm run check && node --test test/*.test.mjs && npm --prefix recorder test"
  },
  "dependencies": {
    "@sparticuz/chromium": "latest",
    "@vercel/blob": "^2.8.0",
    "puppeteer-core": "latest"
  }
}
